# -*- mode: snippet -*-
# name: pdecorator
# group: patterns
# key: pdec
# --
type ${1:Object} ${2:func(int) int}

func LogDecorate(fn $1) $1 {
	return $2 {
		res := fn( // )

		// Do something

        return res
	}
}